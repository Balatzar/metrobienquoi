<div class="container mx-auto px-4 py-8">
  <div class="mb-6">
    <%= link_to '← Back to Search', search_path(@search), class: "text-blue-500 hover:text-blue-700" %>
  </div>

  <div class="bg-white shadow-md rounded-lg p-6">
    <h1 class="text-2xl font-bold mb-6">Edit Search #<%= @search.id %></h1>
    <%= render 'form', search: @search %>
  </div>
</div>
